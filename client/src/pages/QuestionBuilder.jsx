/* Gilded Ledger Form Studio: deliberate changes, visible work states, and structured recovery guidance. */
import { ArrowLeft, GripVertical, LoaderCircle, Plus, SlidersHorizontal, Trash2, UserPlus } from 'lucide-react';
import { useEffect, useState } from 'react';
import { useLocation } from 'wouter';
import { toast } from 'sonner';
import AdminSidebar from '../components/AdminSidebar';
import ConfirmActionModal from '../components/ConfirmActionModal';
import LedgerPicker from '../components/LedgerPicker';
import { api } from '../lib/api';

const typeLabels = { text: 'Short text', textarea: 'Long text', integer: 'Whole number', currency: 'Currency', date: 'Date', select: 'Select list', boolean: 'Yes / No', paceRating: 'Pace rating', accountNumber: 'Account number' };
const blankQuestion = { label: '', inputType: 'text', required: false, options: [] };

export default function QuestionBuilder() {
  const [, navigate] = useLocation();
  const [questions, setQuestions] = useState([]);
  const [branches, setBranches] = useState([]);
  const [teamMembers, setTeamMembers] = useState([]);
  const [draft, setDraft] = useState(null);
  const [branchDraft, setBranchDraft] = useState({ name: '', code: '' });
  const [memberDraft, setMemberDraft] = useState({ fullName: '', daoCode: '', role: 'BDE', branchId: '' });
  const [directoryRequests, setDirectoryRequests] = useState([]);
  const [loading, setLoading] = useState(true);
  const [loadError, setLoadError] = useState('');
  const [confirmation, setConfirmation] = useState(null);
  const [pendingAction, setPendingAction] = useState('');

  async function load() {
    setLoading(true); setLoadError('');
    try {
      const [questionData, branchData, memberData, requestData] = await Promise.all([api('/admin/questions'), api('/admin/branches'), api('/admin/team-members'), api('/admin/directory-requests')]);
      setQuestions(questionData.questions); setBranches(branchData.branches); setTeamMembers(memberData.teamMembers); setDirectoryRequests(requestData.requests);
    } catch (error) {
      if (/session has expired|sign in again/i.test(error.message)) navigate('/admin/login');
      else setLoadError(error.message);
    } finally { setLoading(false); }
  }

  useEffect(() => { load(); }, []);
  function beginCreate() { setDraft(blankQuestion); }
  function requestConfirmation(action) { setConfirmation(action); }

  async function confirmChange() {
    if (!confirmation) return;
    setPendingAction(confirmation.key);
    try { await confirmation.execute(); toast.success(confirmation.successMessage); setConfirmation(null); }
    catch (error) { toast.error(error.message); }
    finally { setPendingAction(''); }
  }

  function requestQuestionSave() {
    if (!draft.label.trim()) { toast.error('Enter a clear question label before saving.'); return; }
    const snapshot = { ...draft };
    requestConfirmation({ key: 'save-question', title: snapshot._id ? 'Save question changes?' : 'Add this question to the daily ledger?', description: snapshot._id ? `The revised “${snapshot.label}” field will apply to all new reports.` : `“${snapshot.label}” will become part of the live daily report form.`, confirmLabel: snapshot._id ? 'Save changes' : 'Add question', successMessage: snapshot._id ? 'Question changes saved to the reporting template.' : 'Question added to the reporting template.', execute: async () => { const saved = await api(snapshot._id ? `/admin/questions/${snapshot._id}` : '/admin/questions', { method: snapshot._id ? 'PUT' : 'POST', body: JSON.stringify(snapshot) }); setQuestions((current) => snapshot._id ? current.map((question) => question._id === saved.question._id ? saved.question : question) : [...current, saved.question]); setDraft(null); } });
  }

  function retire(question) { requestConfirmation({ key: `retire-${question._id}`, title: `Retire “${question.label}”?`, description: 'This removes the field from future reports. Historic submissions and exports will remain unchanged.', confirmLabel: 'Retire question', danger: true, successMessage: 'Question retired from future reports.', execute: async () => { await api(`/admin/questions/${question._id}`, { method: 'DELETE' }); setQuestions((current) => current.filter((item) => item._id !== question._id)); } }); }
  function addBranch(event) { event.preventDefault(); const snapshot = { ...branchDraft }; requestConfirmation({ key: 'add-branch', title: `Add “${snapshot.name}” to the branch list?`, description: 'Workers will be able to select this branch in new daily reports.', confirmLabel: 'Add branch', successMessage: 'Branch added to the canonical list.', execute: async () => { const result = await api('/admin/branches', { method: 'POST', body: JSON.stringify(snapshot) }); setBranches((current) => [...current, result.branch].sort((a, b) => a.name.localeCompare(b.name))); setBranchDraft({ name: '', code: '' }); } }); }
  function addMember(event) { event.preventDefault(); if (!memberDraft.branchId) { toast.error('Choose the staff member’s branch before adding them.'); return; } const snapshot = { ...memberDraft }; const branchName = branches.find((branch) => branch._id === snapshot.branchId)?.name || 'the selected branch'; requestConfirmation({ key: 'add-member', title: `Add ${snapshot.fullName} to ${branchName}?`, description: `${snapshot.daoCode} will be recorded as a ${snapshot.role} and appear in future exports.`, confirmLabel: 'Add to directory', successMessage: 'BDE/DSO added to the managed directory.', execute: async () => { const result = await api('/admin/team-members', { method: 'POST', body: JSON.stringify(snapshot) }); setTeamMembers((current) => [...current, result.member].sort((a, b) => a.fullName.localeCompare(b.fullName))); setMemberDraft({ fullName: '', daoCode: '', role: 'BDE', branchId: '' }); } }); }
  function resolveDirectoryRequest(request, status) { requestConfirmation({ key: `${status}-${request._id}`, title: status === 'reviewed' ? `Mark ${request.fullName} as reviewed?` : `Dismiss ${request.fullName}’s registration?`, description: status === 'reviewed' ? 'This confirms you have seen the request. Add the person to the directory when their details are approved.' : 'The request will leave the active review queue.', confirmLabel: status === 'reviewed' ? 'Mark reviewed' : 'Dismiss request', danger: status === 'dismissed', successMessage: status === 'reviewed' ? 'Registration marked reviewed. Add the BDE/DSO above when ready.' : 'Registration dismissed.', execute: async () => { await api(`/admin/directory-requests/${request._id}/status`, { method: 'POST', body: JSON.stringify({ status }) }); setDirectoryRequests((current) => current.filter((item) => item._id !== request._id)); } }); }

  const branchOptions = branches.filter((branch) => branch.isActive).map((branch) => ({ value: branch._id, label: branch.name }));
  const roleOptions = [{ value: 'BDE', label: 'BDE' }, { value: 'ESO', label: 'ESO' }];
  const inputTypeOptions = Object.entries(typeLabels).map(([value, label]) => ({ value, label }));

  return <div className="admin-layout"><AdminSidebar /><main className="admin-main admin-main--studio"><header className="admin-header"><div><p className="eyebrow">FORM STUDIO</p><h1>Build the <em>daily ledger.</em></h1></div><button className="primary-button" onClick={beginCreate}><Plus size={16} /> Add question</button></header><section className="studio-intro"><SlidersHorizontal size={18} /><p>Every question has a defined type. Counts, amounts, options, and dates remain consistent before they reach Excel.</p></section>{loadError ? <section className="admin-error-state"><strong>Form Studio could not load.</strong><p>{loadError}</p><button className="primary-button" type="button" onClick={load}>Retry connection</button></section> : <><section className="directory-section"><div className="directory-heading"><p className="eyebrow">CANONICAL IDENTITY DIRECTORY</p><h2>Branches and BDE / ESO names</h2><p>These managed choices replace free-text identity fields. DAO codes and roles stay out of the worker form but are carried into every export.</p></div><div className="directory-grid"><form className="directory-panel" onSubmit={addBranch}><div className="directory-panel__header"><span>01</span><h3>Branch list</h3></div><div className="inline-form"><input aria-label="Branch name" placeholder="Branch name" value={branchDraft.name} onChange={(event) => setBranchDraft({ ...branchDraft, name: event.target.value })} required /><input aria-label="Branch code" placeholder="Code (optional)" value={branchDraft.code} onChange={(event) => setBranchDraft({ ...branchDraft, code: event.target.value })} /><button className="primary-button" type="submit" disabled={Boolean(pendingAction)}>{pendingAction === 'add-branch' && <LoaderCircle className="button-spinner" size={15} />} {pendingAction === 'add-branch' ? 'Adding…' : <><Plus size={15} /> Add</>}</button></div><div className="directory-panel__list">{branches.length ? branches.map((branch) => <span key={branch._id}>{branch.name}{branch.code ? <small>{branch.code}</small> : null}</span>) : <em>Add the branches in your region.</em>}</div></form><form className="directory-panel" onSubmit={addMember}><div className="directory-panel__header"><span>02</span><h3>BDE / ESO list</h3></div><div className="member-directory-form"><input aria-label="BDE or ESO name" placeholder="Full name" value={memberDraft.fullName} onChange={(event) => setMemberDraft({ ...memberDraft, fullName: event.target.value })} required /><input aria-label="DAO code" placeholder="DAO code" value={memberDraft.daoCode} onChange={(event) => setMemberDraft({ ...memberDraft, daoCode: event.target.value.toUpperCase() })} required /><LedgerPicker ariaLabel="Select staff branch" className="ledger-picker--compact" disabled={!branchOptions.length || Boolean(pendingAction)} emptyLabel="Add a branch first." onChange={(value) => setMemberDraft({ ...memberDraft, branchId: value })} options={branchOptions} placeholder="Choose branch" value={memberDraft.branchId} /><LedgerPicker ariaLabel="Select staff role" className="ledger-picker--compact" disabled={Boolean(pendingAction)} onChange={(value) => setMemberDraft({ ...memberDraft, role: value })} options={roleOptions} placeholder="Choose role" value={memberDraft.role} /><button className="primary-button" type="submit" disabled={!branches.length || Boolean(pendingAction)}>{pendingAction === 'add-member' && <LoaderCircle className="button-spinner" size={15} />} {pendingAction === 'add-member' ? 'Adding…' : <><UserPlus size={15} /> Add</>}</button></div><div className="directory-panel__list">{teamMembers.length ? teamMembers.map((member) => <span key={member._id}>{member.fullName}<small>{member.daoCode || 'DAO pending'} · {member.role || 'BDE'} · {branches.find((branch) => branch._id === member.branchId)?.name || 'Assigned branch'}</small></span>) : <em>Add each BDE / ESO to their branch.</em>}</div></form></div>{directoryRequests.length > 0 && <section className="directory-requests"><div className="directory-requests__heading"><div><p className="eyebrow">PENDING REGISTRATIONS</p><h3>Names waiting for review</h3></div><span>{directoryRequests.length}</span></div><div className="directory-requests__list">{directoryRequests.map((request) => <article key={request._id}><div><strong>{request.fullName}</strong><span>{request.branchName} · {request.daoCode} · {request.role}</span></div><div><button className="secondary-button" type="button" disabled={Boolean(pendingAction)} onClick={() => resolveDirectoryRequest(request, 'dismissed')}>Dismiss</button><button className="primary-button" type="button" disabled={Boolean(pendingAction)} onClick={() => resolveDirectoryRequest(request, 'reviewed')}>{pendingAction === `reviewed-${request._id}` && <LoaderCircle className="button-spinner" size={15} />}{pendingAction === `reviewed-${request._id}` ? 'Reviewing…' : 'Reviewed'}</button></div></article>)}</div></section>}</section><section className="question-list"><div className="question-list__head"><span>ORDER</span><span>QUESTION</span><span>INPUT TYPE</span><span>REQUIRED</span><span /></div>{loading ? <div className="empty-studio">Loading the daily template…</div> : questions.length ? questions.map((question, index) => <article className="question-row" key={question._id}><span className="question-order"><GripVertical size={17} /> {String(index + 1).padStart(2, '0')}</span><button className="question-label" onClick={() => setDraft(question)}>{question.label}<small>{question.key}</small></button><span className="question-type">{typeLabels[question.inputType] || question.inputType}</span><span className={question.required ? 'required-mark is-required' : 'required-mark'}>{question.required ? 'Required' : 'Optional'}</span><button className="icon-button" aria-label={`Retire ${question.label}`} disabled={Boolean(pendingAction)} onClick={() => retire(question)}>{pendingAction === `retire-${question._id}` ? <LoaderCircle className="button-spinner" size={15} /> : <Trash2 size={16} />}</button></article>) : <div className="empty-studio">The daily template will appear here after the baseline questions are seeded.</div>}</section></>}{draft && <div className="studio-drawer" role="dialog" aria-modal="true"><div className="studio-drawer__backdrop" onClick={() => !pendingAction && setDraft(null)} /><section className="studio-drawer__panel"><button className="drawer-back" disabled={Boolean(pendingAction)} onClick={() => setDraft(null)}><ArrowLeft size={16} /> Back to template</button><p className="eyebrow">{draft._id ? 'EDIT QUESTION' : 'NEW QUESTION'}</p><h2>{draft._id ? 'Refine this field' : 'Add a precise field'}</h2><label className="field"><span className="field__label">Question label</span><input disabled={Boolean(pendingAction)} value={draft.label} onChange={(event) => setDraft({ ...draft, label: event.target.value })} placeholder="e.g. How many accounts were funded?" /></label><label className="field"><span className="field__label">Input type</span><LedgerPicker ariaLabel="Question input type" className="ledger-picker--drawer" disabled={Boolean(pendingAction)} onChange={(value) => setDraft({ ...draft, inputType: value })} options={inputTypeOptions} value={draft.inputType} /></label>{draft.inputType === 'select' && <label className="field"><span className="field__label">Options</span><textarea disabled={Boolean(pendingAction)} rows="4" value={(draft.options || []).map((option) => typeof option === 'string' ? option : option.label).join('\n')} onChange={(event) => setDraft({ ...draft, options: event.target.value.split('\n').map((value) => value.trim()).filter(Boolean) })} placeholder="One option per line" /></label>}<label className="toggle-row"><input disabled={Boolean(pendingAction)} type="checkbox" checked={draft.required} onChange={(event) => setDraft({ ...draft, required: event.target.checked })} /><span>Required for submission</span></label><button className="primary-button" type="button" disabled={Boolean(pendingAction)} onClick={requestQuestionSave}>{pendingAction === 'save-question' && <LoaderCircle className="button-spinner" size={15} />}{pendingAction === 'save-question' ? 'Saving…' : 'Save to daily template'}</button></section></div>}<ConfirmActionModal action={confirmation} busy={Boolean(pendingAction)} onCancel={() => setConfirmation(null)} onConfirm={confirmChange} /></main></div>;
}
